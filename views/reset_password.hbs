
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-header bg-success text-white">
                <h3 class="mb-0"><i class="fas fa-lock-open"></i> تغيير كلمة المرور</h3>
            </div>
            
            <div class="card-body">
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i> تم التحقق بنجاح. يمكنك الآن إنشاء كلمة مرور جديدة
                </div>
                
                <form action="/farmer/reset-password" method="POST">
                    <input type="hidden" name="national_id" value="{{national_id}}">
                    <input type="hidden" name="reset_token" value="{{reset_token}}">
                    
                    <div class="mb-3">
                        <label class="form-label">كلمة المرور الجديدة</label>
                        <input type="password" name="password" class="form-control" minlength="6" required>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">تأكيد كلمة المرور الجديدة</label>
                        <input type="password" name="confirm_password" class="form-control" minlength="6" required>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success">
                            <i class="fas fa-save"></i> حفظ كلمة المرور الجديدة
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
