<div class="card shadow">
    <div class="card-header bg-info text-white">
        <h3 class="mb-0"><i class="fas fa-tachometer-alt me-2"></i> لوحة تحكم المزارع</h3>
    </div>
    
    <div class="card-body">
        <div class="alert alert-primary">
            <h4 class="alert-heading">مرحبا بك، {{session.user.first_name}}!</h4>
            <p class="mb-0">يمكنك هنا متابعة حالة الاعتراضات المقدمة وإدارة حسابك</p>
        </div>
        
        <div class="d-flex justify-content-end mb-4">
            {{#if canSubmitNewObjection}}
            <a href="/objection/new" class="btn btn-primary">
                تقديم اعتراض جديد <i class="fas fa-plus-circle me-2"></i>
            </a>
            {{else}}
            <button class="btn btn-secondary" disabled>
                تقديم اعتراض جديد <i class="fas fa-plus-circle me-2"></i>
                <small>(لديك اعتراض قيد المعالجة)</small>
            </button>
            {{/if}}
        </div>
        
        {{#if objections.length}}
        <div class="table-responsive">
            <table class="table table-striped text-center">
                <thead class="table-dark">
                    <tr>
                        <th>رقم الاعتراض</th>
                        <th>رقم المعاملة</th>
                        <th>الحالة</th>
                        <th>تاريخ التقديم</th>
                        <th>آخر تحديث</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each objections}}
                    <tr>
                        <td>{{code}}</td>
                        <td>{{transaction_number}}</td>
                        <td>
                            <span class="badge bg-{{getBadgeClass status}}">
                                {{arStatus status}}
                            </span>
                        </td>
                        <td dir="ltr" class="text-center">{{formatDate created_at}}</td>
                        <td dir="ltr" class="text-center">{{formatDate updated_at}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
        <div class="text-center py-5">
            <i class="fas fa-clipboard-list fa-4x text-muted"></i>
            <h4 class="my-3">لا توجد اعتراضات مسجلة</h4>
            <p class="text-muted">يمكنك البدء بتقديم اعتراض جديد باستخدام الزر بالأعلى</p>
        </div>
        {{/if}}
    </div>
</div>